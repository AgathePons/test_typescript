/**
 * printable.ts : interface
 * @author AgathePons 2022-10
 * @version 1.0.0
 */

// in interface, just methos, without body
// the interfaces defined here have to be implemanted in classes
export interface Printable {
  toString(): string;
}